[project]
name = "common"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.28.0",
    "psycopg2-binary",
    "langfuse>=4.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/common"]
